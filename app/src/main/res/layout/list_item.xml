<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="5dip" >

    <ImageView
        android:id="@+id/receita_image"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="5dip"
        app:srcCompat="@mipmap/ic_launcher"
        android:contentDescription="@string/image_list_description" />

    <TextView
        android:id="@+id/receita_difficult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@id/receita_image"
        android:textColor="@color/text_color"
        android:maxLines="2"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/receita_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/receita_difficult"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:textColor="@color/text_color"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toEndOf="@id/receita_image"
        android:gravity="center_vertical"
        android:textSize="15sp" />

</RelativeLayout>

